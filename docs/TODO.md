- Make the relationship between Release and output Connection using spec.Parent instead of owner reference. Then create
  output connection in the targetNamespace instead of the release namespace.
- Find a shortcut for ClusterInterface used only once
- Refactor inputs for namespace definition
