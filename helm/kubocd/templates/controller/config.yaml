---
apiVersion: kubocd.kubotal.io/v1alpha1
kind: Config
metadata:
  name: conf01
  namespace: {{ .Release.Namespace }}
spec:
  kuboAppRedirects:
{{- toYaml .Values.config.kuboAppRedirects | nindent 4 }}
  clusterRoles:
{{- toYaml .Values.config.clusterRoles | nindent 4 }}
